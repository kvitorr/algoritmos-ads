//9. Leia 2 números (A, B) e escreva-os em ordem inversa (B, A).

import {input, print} from "./io_utils.js"

function main() {
    
    //Entradas
    const a = Number(input('Digite um número a: '))
    const b = Number(input('Digite um número b: '))

    //Saída
    print(`Ordem inversa (${b}, ${a}) `)

} main()